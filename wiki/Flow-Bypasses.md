# Flow bypasses — superseded

This page has been merged into [Stage flow and selector](Stage-Flow-and-Selector), which is now the canonical owner for the post-legal company/logo bypass, selector-specific automatic-save bypass, rejected broad flow-bypass approaches, and their runtime evidence.

Exact guarded patch bytes and expected/replacement values remain in the [Address and patch-site registry](Address-and-Patch-Site-Registry).

This slug is retained only so historical and external links continue to resolve.
