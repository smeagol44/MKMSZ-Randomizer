# Runtime and memory map

> **Superseded:** continuous ROM/RDRAM ownership, allocation, aliases, lifecycle, proof conflicts, and free/unknown classification now belong to [Memory and allocation map](Memory-and-Allocation-Map).

This old slug is retained so historical links do not break.

For mechanism rather than allocation ownership, see [Core runtime and address database](Core-Runtime-and-Address-Database). For exact guarded edits, see the [Address and patch-site registry](Address-and-Patch-Site-Registry).
