# Legacy Lua implementation

`MKMSZR - 1.1.lua` is preserved as a reference for the original BizHawk-based
randomizer logic. It is not the implementation target of the native ROM rewrite.

Useful policy/logic can be ported deliberately, but emulator-memory workarounds
should not be copied into the new patching core when a native mechanism is known.
